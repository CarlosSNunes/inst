import { ShowActiveProfilePipe } from './show-active-profile.pipe';

describe('ShowActiveProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowActiveProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
